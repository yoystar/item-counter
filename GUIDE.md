# 物品计数标注工具 · 使用说明书

## 工具简介

本工具用于对图片中的物品进行计数标注。上传图片后，可通过**框选识别**或**手动点击**两种方式在物品位置放置带编号的圆形标注，完成后导出带标注的图片。

适用场景：货架盘点、样本数量统计、图片物品清点等需要对图片内容逐一计数的场景。

---

## 界面布局

```
┌─────────────────────────────────┬──────────────────┐
│                                 │   // 物品计数标注  ◑ │
│                                 │ ─────────────────  │
│                                 │  + 上传图片         │
│                                 │ ─────────────────  │
│          画 布 区                │  ◎ 框选识别         │
│     （显示图片与标注）             │  手动模式  ●       │
│                                 │ ─────────────────  │
│                                 │  ▸ 标注样式         │
│                                 │ ─────────────────  │
│                                 │  ⇆ 标注重排序       │
│                                 │  ✕ 清除  ↓ 导出    │
│                                 │ ─────────────────  │
│                                 │  校验状态           │
│                                 │  标注列表           │
└─────────────────────────────────┴──────────────────┘
```

- **左侧画布区**：显示上传的图片，所有标注操作在此进行
- **右侧控制面板**：上传、识别、样式、导出等全部功能入口

---

## 快速开始

### 第一步：上传图片

点击右侧面板顶部的 **`+ 上传图片`** 按钮，选择本地图片文件。

- 支持格式：PNG、JPG、JPEG、GIF、BMP、WEBP
- 单文件最大：128 MB
- 上传成功后图片显示在左侧画布，右侧各功能按钮自动解锁

> 上传新图片时，当前所有标注会被清空。

### 第二步：添加标注

提供两种标注方式，可根据图片特点选择或混合使用。

---

## 标注方式一：框选识别（推荐用于批量重复物品）

适合图片中存在大量**外观相似**物品的情况，一次操作可识别全图所有同类物品。

**操作步骤：**

1. 点击 **`◎ 框选识别`** 按钮，进入框选模式（手动模式自动关闭）
2. 在画布上**按住鼠标拖拽**，框选任意一个目标物品作为样本
3. 右侧面板展开识别设置区域

   | 参数 | 范围 | 默认值 | 说明 |
   |------|------|--------|------|
   | 相似度阈值 | 0.30 – 0.95 | 0.35 | 值越低匹配越多，误检也越多；值越高匹配越少，漏检也越少 |

4. 调整阈值后点击 **`确认识别`**
5. 识别过程中画布中央弹出进度框，显示处理步骤
6. 识别完成后自动在所有匹配位置添加标注，旧标注同时被替换

**识别过程中按 `Esc`** 可立即取消识别，恢复到框选前状态。

**调参建议：**
- 漏检（部分物品没有被标注）→ 降低阈值
- 误检（非目标区域被误标）→ 提高阈值
- 框选样本时尽量选取**完整且清晰**的单个物品

---

## 标注方式二：手动点击

适合物品数量较少、或自动识别效果不理想需要逐一确认的情况。

**操作步骤：**

1. 确认右侧 **`手动模式`** 开关处于**开启**状态（默认开启）
2. 在画布上**点击**任意位置，该位置自动添加一个带编号的圆形标注
3. 编号按添加顺序自动递增（1、2、3…）

---

## 标注管理

### 移动标注

在手动模式关闭时（或直接对标注操作），**按住标注拖拽**即可移动到新位置。移动后右侧列表中的坐标同步更新。

### 删除单个标注

**点击**已有标注圆圈，该标注被删除，其后所有标注自动重新编号。

也可以在右侧标注列表中点击对应条目右侧的 **`×`** 按钮删除。

### 撤销操作

按 **`Ctrl + Z`**（Mac 为 `⌘ + Z`）撤销上一步操作，最多可撤销 30 步。

支持撤销的操作：添加标注、删除标注、框选识别批量添加、重排序、清除全部。

### 重排序

点击 **`⇆ 标注重排序`**，按**从上到下、从左到右**的阅读顺序重新对所有标注编号。适合手动添加后编号顺序不整齐的情况。

### 清除全部

点击 **`✕ 清除`** 删除所有标注（可用 Ctrl+Z 撤销）。

---

## 标注样式

点击右侧面板的 **`▸ 标注样式`** 展开样式设置面板：

| 设置项 | 范围 | 默认值 | 说明 |
|--------|------|--------|------|
| 标注大小 | 1 – 120 | 50 | 圆形标注的半径（像素，相对原图） |
| 不透明度 | 10% – 100% | 60% | 圆形填充的透明程度 |
| 标注颜色 | 任意颜色 | 橙红色 | 圆圈边框及填充的基础色 |
| 数字颜色 | 任意颜色 | 米白色 | 圆圈内编号数字的颜色 |

样式调整后，画布上**所有已有标注**实时更新。

---

## 重叠警告

当两个标注的中心距离过近时，相关标注会自动变为**黄色**，提示可能存在重复标注同一物品的情况。解决方法：删除多余标注，或调小标注大小。

---

## 校验状态

右侧面板底部实时显示当前标注的状态：

| 状态 | 含义 |
|------|------|
| `— 尚未标注` | 还没有添加任何标注 |
| `✓ 编号正常，共 N 个物品` | 所有编号连续，计数有效 |
| `✕ 编号异常，请检查` | 编号不连续，建议执行重排序 |

---

## 导出

点击 **`↓ 导出`** 按钮，将**当前画布显示的内容**（包括图片底图和所有标注）保存为 PNG 文件下载到本地，文件名为 `annotated.png`。

导出内容与界面完全一致，包括：
- 标注的大小、颜色、不透明度
- 发光效果与阴影
- 重叠标注的黄色警告色

---

## 主题切换

点击右侧面板右上角的 **`◑`** / **`◐`** 按钮，在**深色模式**和**蓝白模式**之间切换。主题偏好自动保存，下次打开时恢复上次的选择。

---

## 快捷键汇总

| 按键 | 功能 |
|------|------|
| `Ctrl + Z` | 撤销上一步操作 |
| `Esc` | 取消正在进行的框选识别或导出 |

---

## 注意事项

- 上传新图片时当前所有标注会被清空，请先导出
- 服务器自动清理：超过 30 天的图片会在下次上传时被删除；图片总数超过 100 张时自动删除最旧的
- 导出的图片分辨率与画布显示分辨率一致（图片在界面中的缩放尺寸）
