# 涵点 · 物品计数标注 · 使用说明

## 工具简介

**涵点**是一款图片物品计数标注工具。上传图片后，在图片上逐一点击（或自动识别）来放置带编号的圆形标注，完成后导出带标注的图片。

适用场景：货架盘点、样本统计、图片清点等需要对图片内容逐一计数的场景。

---

## 界面一览

```
┌──────────────────────────────────┬─────────────────────┐
│                                  │  物品计数标注工具  ◑  │
│                                  │ ─────────────────── │
│                                  │  + 上传图片          │
│                                  │ ─────────────────── │
│          画 布 区                 │  ◎ 框选识别          │
│   （上传图片后在此点击标注）        │  手动模式  ●        │
│                                  │ ─────────────────── │
│                                  │  ▸ 标注样式          │
│                                  │ ─────────────────── │
│                                  │  ⇆ 标注重排序        │
│                                  │  ✕ 清除  ↓ 导出     │
│                                  │ ─────────────────── │
│                                  │  校验状态            │
│                                  │  标注列表            │
└──────────────────────────────────┴─────────────────────┘
```

左侧为**画布区**（操作图片），右侧为**控制面板**（所有功能入口）。右上角 `◑` / `◐` 按钮可切换深色 / 蓝白主题。

---

## 第一步：上传图片

支持以下四种方式，任选其一：

| 方式 | 操作 |
|------|------|
| 点击上传 | 点击右侧 **`+ 上传图片`** 按钮，或点击画布中央的提示文字 |
| 拖拽上传 | 将图片文件直接**拖入**左侧画布区域 |
| 粘贴上传 | 复制图片后按 **`Ctrl + V`** |
| 右键粘贴 | 在画布区**右键** → 点击「粘贴图片」 |

支持格式：PNG / JPG / JPEG / GIF / BMP / WEBP，单文件最大 128 MB。

> 上传新图片时，当前所有标注会被清空，请先导出。

---

## 第二步：添加标注

### 方式一：手动点击（少量物品 / 精确控制）

确认右侧**手动模式**开关已开启（默认开启），然后直接**点击画布**上的物品位置，每次点击自动添加一个带编号的圆形标注（1、2、3…依次递增）。

### 方式二：框选识别（批量重复物品）

适合图片中存在大量**外观相似**物品的情况。

1. 点击 **`◎ 框选识别`** 进入框选模式
2. 在画布上**按住鼠标拖拽**，框住任意一个目标物品作为样本
3. 右侧出现**相似度阈值**滑块（默认 0.35）

   - 漏检（有物品没标到）→ **降低**阈值
   - 误检（标到了非目标）→ **提高**阈值

4. 点击 **`确认识别`**，识别完成后自动在所有匹配位置添加标注

识别过程中按 `Esc` 可取消。

---

## 第三步：管理标注

| 操作 | 方法 |
|------|------|
| 移动标注 | 关闭手动模式后，按住标注拖拽到新位置 |
| 删除单个标注 | 点击标注圆圈，或点击右侧列表中的 `×` |
| 撤销 | `Ctrl + Z`（最多 30 步） |
| 重排序编号 | 点击 **`⇆ 标注重排序`**，按从上到下、从左到右重新编号 |
| 清除全部 | 点击 **`✕ 清除`**（可用 Ctrl+Z 撤销） |

---

## 标注样式调整

点击 **`▸ 标注样式`** 展开设置：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 标注大小 | 50 | 圆形半径（像素），范围 1 – 120 |
| 不透明度 | 60% | 圆形填充的透明程度 |
| 标注颜色 | 橙红色 | 圆圈边框及填充色 |
| 数字颜色 | 米白色 | 圆圈内数字颜色 |

样式修改后，所有已有标注实时更新。

> **重叠警告**：当两个标注距离过近时自动变为**黄色**，提示可能重复标注了同一物品。

---

## 第四步：导出

点击 **`↓ 导出`**，将当前画布（图片 + 所有标注）保存为 `annotated.png` 下载到本地。

---

## 快捷键

| 按键 | 功能 |
|------|------|
| `Ctrl + V` | 粘贴图片上传 |
| `Ctrl + Z` | 撤销上一步 |
| `Esc` | 取消正在进行的框选识别 |
